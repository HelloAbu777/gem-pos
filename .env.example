# Database (Production: Use Vercel Postgres or Neon)
DATABASE_URL="postgresql://postgres:PAROL@localhost:5432/gem_pos?schema=public"

# Auth (Production: Generate new secret with: openssl rand -base64 32)
NEXTAUTH_SECRET="gem-pos-secret-key-2024-very-secure"

# URL (Production: Replace with your domain)
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
